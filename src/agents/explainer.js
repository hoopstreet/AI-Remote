const axios = require(\"axios\");\n\nasync function explainTask(task) {\n  const prompt = `\n    You are an AI assistant specialized in explaining technical tasks.\n    Explain the following Task.md in simple, yet technically accurate terms.\n    Your explanation should cover:\n    - Why this task is needed (context/problem it solves)\n    - What parts of the system it affects (e.g., frontend, backend, database, specific modules)\n    - The estimated risk level (briefly justify)\n    - The expected outcome or benefit upon completion.\n\n    TASK:\n    \`\`\`\n    ${task}\n    \`\`\`\n\n    Example Output:\n    This task is needed to address a performance bottleneck in the user authentication flow. It will primarily affect the backend authentication service and the database, specifically the user sessions table. The risk is estimated as MEDIUM because it involves changes to a critical path, but it's not a full rewrite. The expected outcome is a 20% reduction in login times and improved user experience.\n    `;\n\n  try {\n    const res = await axios.post(\n      \"https://api.openai.com/v1/chat/completions\",\n      {\n        model: \"gpt-4o-mini\",\n        messages: [{ role: \"user\", content: prompt }],\n        temperature: 0.7,\n      },\n      {\n        headers: {\n          Authorization: `Bearer ${process.env.OPENAI_API_KEY}`,\n          \"Content-Type\": \"application/json\",\n        },\n      }\n    );\n    return res.data.choices[0].message.content.trim();\n  } catch (error) {\n    console.error(\"Error in explainer agent:\", error.message);\n    return \"Failed to generate explanation.\";\n  }\n}\n\nmodule.exports = { explainTask };\n