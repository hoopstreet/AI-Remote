name: Container & AI Distro
on:
  push:
    tags:
      - 'v*'
jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Docker Build & Push
        if: env.DOCKER_READY == 'true'
        run: |
          echo "Building Docker Image for AI-Remote..."
          # docker build -t ${{ secrets.DOCKER_USERNAME }}/ai-remote:${{ github.ref_name }} .
          # docker push ${{ secrets.DOCKER_USERNAME }}/ai-remote:${{ github.ref_name }}
      - name: Hugging Face Sync
        if: env.HF_READY == 'true'
        run: |
          echo "Syncing to Hugging Face..."
          # git push https://hoopstreet:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/hoopstreet/ai-remote main
