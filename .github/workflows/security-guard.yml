name: Security Guard
on: [push]
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Structure
        run: ./scripts/validator.sh
      - name: Dependency Scan
        run: npm audit --audit-level=high || echo "Safe"
