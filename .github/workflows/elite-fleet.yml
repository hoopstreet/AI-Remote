name: 🛡️ AI-Remote Elite Fleet
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 * * * *'

jobs:
  autonomous-operation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Dependencies
        run: npm install
      - name: 🚀 Run Elite Fleet (Sentinel + Bot)
        env:
          # This maps your GitHub Secrets to the Node.js process
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_ANON_KEY || secrets.SUPABASE_KEY }}
        run: node src/main.js
